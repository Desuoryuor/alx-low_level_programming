#include <stdio.h>
/**
 * main - This code prints the Alphabets in Lowercase
 * Return: Ale]ways 0 (Success)
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
